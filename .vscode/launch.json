{
  "configurations": [
    {
      "type": "firefox",
      "request": "launch",
      "name": "Launch firefox against localhost",
      "url": "http://localhost:3000",
      "webRoot": "${workspaceFolder}",
      "pathMappings": [
        {
          "url": "http://localhost:3000/workspaces/React-tutorial/src",
          "path": "${workspaceFolder}/src"
        }
      ]
    }
  ]
}